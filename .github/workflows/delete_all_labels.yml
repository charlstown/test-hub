name: Delete all labels
on: workflow_dispatch

jobs:
  delete-labels:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
    - name: Get current labels
      id: get-labels
      uses: actions/github-script@v7
      with:
        script: |
          const labels = await github.rest.issues.listLabelsForRepo({
            owner: context.repo.owner,
            repo: context.repo.repo
          });
          console.log(labels.data);  // Add this line to print the labels data
          return labels.data;

    - name: Print labels
      run: echo "${{ steps.get-labels.outputs.labels }}"

    - name: Delete labels
      if: ${{ steps.get-labels.outputs.labels.length > 0 }}
      run: |
        const labels = ${{ toJSON(steps.get-labels.outputs.labels) }};
        for (const label of labels) {
          await github.rest.issues.deleteLabel({
            owner: context.repo.owner,
            repo: context.repo.repo,
            name: label.name
          });
        }
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
